# Production requirements - extends requirements-runpod.txt
-r requirements-runpod.txt

# Production infrastructure
redis>=5.0.0
celery>=5.3.0
gunicorn>=21.0.0

# Error monitoring and logging
sentry-sdk[streamlit]>=1.40.0
structlog>=23.2.0

# Performance and caching
hiredis>=2.2.0  # Faster Redis client
psutil>=5.9.0   # System monitoring

# Rate limiting and security
slowapi>=0.1.9
python-multipart>=0.0.6

# Business analytics
mixpanel>=4.10.0
# posthog>=3.0.0  # Alternative to Mixpanel

# File upload optimization
tus-py>=1.3.0

# Accessibility testing
axe-selenium>=2.1.6

# Prometheus metrics
prometheus-client>=0.19.0

# Health checks and monitoring
httpx>=0.25.0  # Better HTTP client for health checks

# Production optimizations
orjson>=3.9.0  # Already in runpod requirements
uvloop>=0.19.0; sys_platform != "win32"  # Faster event loop 