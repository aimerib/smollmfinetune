# Core dependencies
streamlit>=1.24.0
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
accelerate>=0.20.0
peft>=0.4.0
bitsandbytes>=0.41.0

# Data processing and visualization
pandas>=1.5.0
numpy>=1.24.0
plotly>=5.14.0
streamlit-option-menu>=0.3.2

# API and networking
aiohttp>=3.8.0
requests>=2.28.0

# Optional monitoring dependencies (install separately if needed)
# tensorboard>=2.13.0
# wandb>=0.15.0

# Enhanced visualization
matplotlib>=3.7.0

# Development utilities
python-dotenv>=1.0.0

# Optional inference engines (install based on deployment target)
# For high-performance GPU deployment:
vllm>=0.2.0; sys_platform != "darwin"

# For CPU deployment or as fallback:
llama-cpp-python>=0.2.0

sentencepiece>=0.1.99  # needed for translation models